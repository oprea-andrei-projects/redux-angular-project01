import { SortAnimalsPipe } from './sort-animals.pipe';

describe('SortAnimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAnimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
