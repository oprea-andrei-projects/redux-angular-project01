import { SortAnimalsDescPipe } from './sort-animals-desc.pipe';

describe('SortAnimalsDescPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAnimalsDescPipe();
    expect(pipe).toBeTruthy();
  });
});
